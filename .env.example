Copy this file to .env and fill in your own values

JSON Web Token secret key

JWT_SECRET=YOUR_JWT_SECRET

Node environment (development, production, etc.)

NODE_ENV=development

MongoDB connection URI (Local or Atlas)

For Atlas, URL-encode any special characters in your password

MONGO_URI=mongodb+srv://:@.mongodb.net/?retryWrites=true&w=majority

SMTP settings for sending emails (production only)

SMTP_HOST=smtp.yourmailprovider.com
SMTP_PORT=587                 # 465 for SSL, 587 for TLS
SMTP_SECURE=false             # false for TLS (587), true for SSL (465)
SMTP_USER=your_smtp_username
SMTP_PASS=your_smtp_password

Sender address (display name and email)

EMAIL_FROM="Sales Report noreply@yourdomain.com"

RabbitMQ connection URL and queue name

RABBITMQ_URL=amqp://localhost
DAILY_SALES_REPORT_QUEUE=daily_sales_report